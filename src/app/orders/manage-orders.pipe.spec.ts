import { ManageOrdersPipe } from './manage-orders.pipe';

describe('ManageOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new ManageOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
